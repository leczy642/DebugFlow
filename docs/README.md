# Docs: debugFlow

- `architecture.md` — System and RAG pipeline design
- `api_spec.md` — REST API endpoints and schemas

To develop:
- Start backend (port 4000) and frontend (port 3000)
- Configure `.env` for backend to access OpenAI and vector DB
